#include <stdio.h>

struct Array{
    int A[20];
    int size;
    int length;
};

void Merge(struct Array *arr1, struct Array *arr2){
    int i, j, k;
    struct Array *arr3;
    while(i<arr1->length && arr->length){
        if(arr1->A[i]<arr2->A[j]){
            arr3->A[k++] = Arr1
        }
    }
}

int main(){
    struct Array arr1 = {{2,6,10,15,25},10,5};
    struct Array arr2 = {{3,4,7,18,20},10,5};
    struct Array *arr3;
}