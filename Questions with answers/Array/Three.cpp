// Question: Given an array of integers representing an elevation map where the width of each bar is 1, return how much rainwater can be trapped. ( this question is same as the leetcode question no 42. Trapping rain water)
#include<bits/stdc++.h>
using namespace std;

int rainwaterTrapped(vector<int>&arr){

}



int main(){
    vector<int>arr;
    arr.push_back(0);
    arr.push_back(1);
    arr.push_back(0);
    arr.push_back(2);
    arr.push_back(1);
    arr.push_back(0);
    arr.push_back(3);
    arr.push_back(1);
    arr.push_back(0);
    arr.push_back(1);
    arr.push_back(2);

    for(int i = 0; i < arr.size(); i++){
        cout<<arr[i]<<endl;
    }

    return 0;
}