#include<bits/stdc++.h>
using namespace std;

int main(){
    // In this program we have as much cout as we want but all these will be count as the O(1) which means it has a constant time.
    cout<<"I am executing in the constant time, So my complexity is big oh of one."<<endl;
    return 0;
}